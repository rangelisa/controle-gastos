# =====================================
# Banco de dados (suas configurações)
# =====================================
spring.datasource.url=jdbc:mysql://localhost:3307/gastos_db
spring.datasource.username=root
spring.datasource.password=1234

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# =====================================
# Dialeto MySQL (evita warnings no console)
# =====================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =====================================
# JWT Secret
# Em produção, troque por uma variável de ambiente:
#   export JWT_SECRET=sua-chave-muito-segura-aqui
# =====================================
jwt.secret=${JWT_SECRET:isabella-gastos-chave-secreta-2024-xk}